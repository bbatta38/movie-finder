// import

// action

// action creator

// API actions

// initialState
const initialState = {};
// reducer

const reducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

// reducer functions

// export

// export default

export default reducer;
